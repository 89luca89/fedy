{
	"icon": "enpass",
	"label": "Enpass",
	"description": "Enpass is a Freemium Offline password management program to securely store passwords",
	"license": "Custom",
	"libre": false,
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "test -f /opt/Enpass/runenpass.sh" }
	}
}
