{
	"icon":"uget-icon",
	"label": "uGet",
	"description": "Download manager using GTK+ and libcurl",
	"license": "GPLv3",
	"libre": true,
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "rpm --quiet --query uget" }
	}
}
